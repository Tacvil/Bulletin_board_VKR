package com.example.bulletin_board.domain.search

import com.example.bulletin_board.domain.TextWatcherCallback

interface InitTextWatcher {
    fun initTextWatcherHelperl(callback: TextWatcherCallback)
}
